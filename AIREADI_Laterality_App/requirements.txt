# Baked into the MAP by `monai-deploy package`.
# No torchvision here on purpose: TorchScript serialises the model source into model.ts,
# so the architecture library is not needed at inference time.
monai>=1.2.0
Pillow>=8.4.0
pydicom>=2.3.0
highdicom>=0.18.2
SimpleITK>=2.0.0
setuptools>=59.5.0 # for pkg_resources
monai-deploy-app-sdk==4.0.0
holoscan-cu13==4.2.0
